{
  "name": "@speq-ai/speq",
  "publishConfig": {
    "access": "public"
  },
  "version": "0.1.4",
  "description": "CLI companion for the SpeQ architectural specification format",
  "license": "MIT",
  "author": "speq",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/speq-ai/speq-tools.git"
  },
  "keywords": [
    "speq",
    "spec",
    "architecture",
    "ai",
    "cli"
  ],
  "type": "module",
  "bin": {
    "speq": "dist/index.js"
  },
  "files": [
    "dist/index.js",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "prebuild": "node -e \"import('fs').then(fs=>{try{fs.rmSync('dist',{recursive:true})}catch{}})\"",
    "build": "bun build src/index.ts --outfile dist/index.js --target node --packages=external --format esm && node -e \"import('fs').then(fs=>{const f='dist/index.js';const c=fs.readFileSync(f,'utf8');if(!c.startsWith('#!/'))fs.writeFileSync(f,'#!/usr/bin/env node\\n'+c);fs.chmodSync(f,0o755);})\"",
    "prepare": "npm run build",
    "binary:mac": "bun build src/index.ts --compile --outfile releases/speq-macos-arm64 --target bun-darwin-arm64",
    "binary:linux": "bun build src/index.ts --compile --outfile releases/speq-linux-x64 --target bun-linux-x64",
    "binary:win": "bun build src/index.ts --compile --outfile releases/speq-windows-x64.exe --target bun-windows-x64",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.3.0",
    "@noble/ciphers": "^2.1.1",
    "chalk": "^5.4.1",
    "commander": "^14.0.3",
    "ora": "^9.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "eslint": "^10.0.3",
    "typescript": "^5.8.3"
  }
}
